'use client';

import { createTheme } from '@mantine/core';

export const theme = createTheme({
  /* Put your mantine theme override here */
  colors: {
    teal: ['#E6F7F7', '#B3E0E0', '#80C9C9', '#4DB2B2', '#269999', '#008080', '#006666', '#004D4D', '#003333', '#001A1A'],
  },
  primaryColor: 'teal',
});
